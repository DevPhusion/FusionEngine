#version 330 core
in vec2 TexCoord;
out vec4 FragColor;

void main() {
    vec2 c = TexCoord * 2.0 - 1.0;
    float r2 = dot(c, c);
    if (r2 > 1.0) discard;
    float falloff = (1.0 - r2) * (1.0 - r2); // poly6-ish smooth kernel
    FragColor = vec4(falloff, 0.0, 0.0, 1.0);
}
